/* ======================================================
   Aero Space - Liquid Glassmorphism Theme 10.0
   Hero + Sidebar Full Upgrade
   ====================================================== */

/* ------------------------------------------------------
   1. 基础变量
   ------------------------------------------------------ */
:root {
  --radius-xl: 32px;
  --radius-lg: 20px;
  --radius-md: 12px;
  --radius-pill: 50px;

  --sl-color-accent-low: #1e144a;
  --sl-color-accent: #a78bfa;
  --sl-color-accent-high: #ddd6fe;

  --glass-bg: rgba(20, 21, 30, 0.75);
  --glass-border: rgba(255, 255, 255, 0.1);
  --liquid-glow: rgba(167, 139, 250, 0.4);
  --liquid-glow-strong: rgba(167, 139, 250, 0.6);

  --transition-liquid: all 0.5s cubic-bezier(0.23, 1, 0.32, 1);
  --transition-fast: all 0.25s cubic-bezier(0.23, 1, 0.32, 1);
}

:root[data-theme='dark'] {
  --sl-color-bg: #0b0d12;
}

/* ======================================================
   2. 顶部导航栏（双层修复保留）
   ====================================================== */
site-header {
  background: transparent !important;
  border: none !important;
  box-shadow: none !important;
  backdrop-filter: none !important;
  -webkit-backdrop-filter: none !important;
  padding: 0 !important;
  margin: 0 !important;
  height: 0 !important;
  overflow: visible !important;
  position: static !important;
}

header.header {
  background: transparent !important;
  background-color: transparent !important;
  border: none !important;
  box-shadow: none !important;
  backdrop-filter: none !important;
  -webkit-backdrop-filter: none !important;
  padding: 0 !important;
  height: auto !important;
  overflow: visible !important;
  position: static !important;
}

div.header {
  position: fixed !important;
  top: 16px !important;
  left: 50% !important;
  right: auto !important;
  transform: translateX(-50%) !important;
  width: 92% !important;
  max-width: 1400px !important;
  padding: 0 40px !important;
  height: 64px !important;
  border-radius: var(--radius-pill) !important;
  background-color: var(--glass-bg) !important;
  backdrop-filter: blur(24px) saturate(180%) !important;
  -webkit-backdrop-filter: blur(24px) saturate(180%) !important;
  border: 1px solid var(--glass-border) !important;
  box-shadow: 0 20px 40px -15px rgba(0, 0, 0, 0.8),
              0 0 20px -5px var(--liquid-glow) !important;
  transition: var(--transition-liquid) !important;
  z-index: 1000 !important;
  display: flex !important;
  align-items: center !important;
}

.site-title {
  color: #fff !important;
  font-weight: 800 !important;
  letter-spacing: -0.02em;
}

div.header:hover {
  background-color: rgba(20, 21, 30, 0.85) !important;
  transform: translateX(-50%) translateY(-2px) !important;
  border-color: rgba(167, 139, 250, 0.3) !important;
  box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.9),
              0 0 30px -5px var(--liquid-glow) !important;
}

/* 滚动进度条 */
@supports (animation-timeline: scroll()) {
  div.header::after {
    content: "";
    position: absolute;
    bottom: 0;
    left: 40px;
    right: 40px;
    height: 2px;
    background: linear-gradient(90deg, var(--sl-color-accent), #d8b4fe);
    border-radius: 2px;
    transform-origin: left;
    animation: timelinePlayhead linear;
    animation-timeline: scroll(root);
  }
}
@keyframes timelinePlayhead {
  0% { transform: scaleX(0); }
  100% { transform: scaleX(1); }
}

/* ======================================================
   3. HERO 区域全面升级
   ====================================================== */

/* Hero 容器：背景星云光效 */
.hero {
  position: relative !important;
  overflow: hidden !important;
  padding: 6rem 2rem 5rem !important;
}

/* 星云光晕背景层 */
.hero::before {
  content: "";
  position: absolute;
  top: -20%;
  left: 50%;
  transform: translateX(-50%);
  width: 800px;
  height: 600px;
  background: radial-gradient(
    ellipse at center,
    rgba(167, 139, 250, 0.12) 0%,
    rgba(109, 40, 217, 0.06) 40%,
    transparent 70%
  );
  pointer-events: none;
  z-index: 0;
  animation: nebulaFloat 8s ease-in-out infinite;
}

/* 第二个光晕，错位叠加 */
.hero::after {
  content: "";
  position: absolute;
  top: 10%;
  right: 10%;
  width: 400px;
  height: 400px;
  background: radial-gradient(
    ellipse at center,
    rgba(196, 181, 253, 0.08) 0%,
    transparent 65%
  );
  pointer-events: none;
  z-index: 0;
  animation: nebulaFloat 12s ease-in-out infinite reverse;
}

@keyframes nebulaFloat {
  0%, 100% { transform: translateX(-50%) translateY(0) scale(1); }
  33% { transform: translateX(-50%) translateY(-15px) scale(1.03); }
  66% { transform: translateX(-50%) translateY(8px) scale(0.98); }
}

/* Hero 内容层级修复 */
.hero > * {
  position: relative;
  z-index: 1;
}

/* Hero 标题：打字机光标效果 */
.hero h1,
[data-page-title],
.sl-hero-title,
.hero .sl-flex h1 {
  font-size: clamp(2.5rem, 6vw, 4.5rem) !important;
  font-weight: 900 !important;
  letter-spacing: -0.03em !important;
  line-height: 1.1 !important;
  background: linear-gradient(
    135deg,
    #ffffff 0%,
    #ddd6fe 40%,
    #a78bfa 70%,
    #7c3aed 100%
  ) !important;
  -webkit-background-clip: text !important;
  -webkit-text-fill-color: transparent !important;
  background-clip: text !important;
  animation: titleReveal 1.2s cubic-bezier(0.23, 1, 0.32, 1) both;
  position: relative;
}

@keyframes titleReveal {
  0% {
    opacity: 0;
    transform: translateY(30px);
    filter: blur(8px);
  }
  100% {
    opacity: 1;
    transform: translateY(0);
    filter: blur(0);
  }
}

/* Tagline：玻璃胶囊样式 */
.hero .tagline,
.sl-hero-tagline {
  display: inline-flex !important;
  align-items: center !important;
  gap: 8px !important;
  margin: 1.2rem auto !important;
  padding: 10px 24px !important;
  background: rgba(167, 139, 250, 0.08) !important;
  border: 1px solid rgba(167, 139, 250, 0.25) !important;
  border-radius: var(--radius-pill) !important;
  backdrop-filter: blur(12px) !important;
  -webkit-backdrop-filter: blur(12px) !important;
  color: #c4b5fd !important;
  font-size: 1.05rem !important;
  font-weight: 500 !important;
  letter-spacing: 0.01em !important;
  animation: taglineReveal 1.2s cubic-bezier(0.23, 1, 0.32, 1) 0.2s both;
  box-shadow: 0 0 20px -5px rgba(167, 139, 250, 0.2),
              inset 0 1px 0 rgba(255, 255, 255, 0.05) !important;
}

@keyframes taglineReveal {
  0% {
    opacity: 0;
    transform: translateY(20px) scale(0.95);
  }
  100% {
    opacity: 1;
    transform: translateY(0) scale(1);
  }
}

/* Hero 按钮区 */
.hero .sl-flex.actions {
  animation: actionsReveal 1.2s cubic-bezier(0.23, 1, 0.32, 1) 0.4s both;
  gap: 16px !important;
  margin-top: 2rem !important;
}

@keyframes actionsReveal {
  0% { opacity: 0; transform: translateY(20px); }
  100% { opacity: 1; transform: translateY(0); }
}

/* Primary 按钮：液态光泽 */
.hero a[data-variant='primary'],
.hero .sl-link-button[data-variant='primary'] {
  position: relative !important;
  background: linear-gradient(135deg, #7c3aed 0%, #a78bfa 50%, #7c3aed 100%) !important;
  background-size: 200% 200% !important;
  border: none !important;
  border-radius: var(--radius-pill) !important;
  padding: 14px 32px !important;
  font-weight: 700 !important;
  font-size: 1rem !important;
  color: #fff !important;
  overflow: hidden !important;
  transition: var(--transition-liquid) !important;
  box-shadow: 0 8px 25px -5px rgba(124, 58, 237, 0.5),
              0 0 0 1px rgba(255, 255, 255, 0.1) !important;
  animation: backgroundShift 4s ease infinite !important;
}

@keyframes backgroundShift {
  0% { background-position: 0% 50%; }
  50% { background-position: 100% 50%; }
  100% { background-position: 0% 50%; }
}

/* 液态光泽扫光 */
.hero a[data-variant='primary']::before,
.hero .sl-link-button[data-variant='primary']::before {
  content: "";
  position: absolute;
  top: 0;
  left: -100%;
  width: 60%;
  height: 100%;
  background: linear-gradient(
    90deg,
    transparent,
    rgba(255, 255, 255, 0.3),
    transparent
  );
  transition: left 0.6s ease;
  pointer-events: none;
}

.hero a[data-variant='primary']:hover::before,
.hero .sl-link-button[data-variant='primary']:hover::before {
  left: 150%;
}

.hero a[data-variant='primary']:hover,
.hero .sl-link-button[data-variant='primary']:hover {
  transform: translateY(-3px) scale(1.02) !important;
  box-shadow: 0 15px 35px -5px rgba(124, 58, 237, 0.6),
              0 0 30px -5px var(--liquid-glow) !important;
}

/* Minimal 按钮：玻璃描边 */
.hero a[data-variant='minimal'],
.hero .sl-link-button[data-variant='minimal'] {
  background: transparent !important;
  border: 1px solid rgba(167, 139, 250, 0.3) !important;
  border-radius: var(--radius-pill) !important;
  padding: 14px 28px !important;
  color: #c4b5fd !important;
  font-weight: 600 !important;
  transition: var(--transition-liquid) !important;
  backdrop-filter: blur(8px) !important;
}

.hero a[data-variant='minimal']:hover,
.hero .sl-link-button[data-variant='minimal']:hover {
  background: rgba(167, 139, 250, 0.1) !important;
  border-color: rgba(167, 139, 250, 0.6) !important;
  transform: translateY(-2px) !important;
  box-shadow: 0 8px 20px -5px rgba(167, 139, 250, 0.3) !important;
  color: #fff !important;
}

/* Hero 图片：悬浮旋转 */
.hero img,
.hero .hero-image img,
.sl-hero-image img {
  animation: heroFloat 6s ease-in-out infinite !important;
  filter: drop-shadow(0 20px 40px rgba(167, 139, 250, 0.3)) !important;
  transition: filter 0.5s ease !important;
}

.hero img:hover,
.hero .hero-image img:hover {
  filter: drop-shadow(0 25px 50px rgba(167, 139, 250, 0.5)) !important;
  animation-play-state: paused !important;
}

@keyframes heroFloat {
  0%, 100% {
    transform: translateY(0) rotate(0deg);
  }
  25% {
    transform: translateY(-12px) rotate(2deg);
  }
  75% {
    transform: translateY(-6px) rotate(-1.5deg);
  }
}

/* ======================================================
   4. CardGrid 技能卡片升级
   ====================================================== */
.card,
.sl-card {
  border-radius: var(--radius-lg) !important;
  background: rgba(20, 21, 30, 0.6) !important;
  border: 1px solid rgba(255, 255, 255, 0.06) !important;
  backdrop-filter: blur(16px) !important;
  transition: var(--transition-liquid) !important;
  position: relative !important;
  overflow: hidden !important;
}

/* 卡片顶部发光线 */
.card::before,
.sl-card::before {
  content: "";
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, transparent, rgba(167, 139, 250, 0.4), transparent);
  opacity: 0;
  transition: opacity 0.5s ease;
}

.card:hover::before,
.sl-card:hover::before {
  opacity: 1;
}

.card:hover,
.sl-card:hover {
  transform: translateY(-8px) !important;
  border-color: rgba(167, 139, 250, 0.2) !important;
  box-shadow: 0 20px 40px -10px rgba(0, 0, 0, 0.6),
              0 0 30px -10px var(--liquid-glow) !important;
  background: rgba(25, 26, 38, 0.8) !important;
}

/* ======================================================
   5. 侧边栏全面升级
   ====================================================== */
sidebar-drawer {
  background: transparent !important;
  border: none !important;
}

.sidebar-pane {
  padding-top: 96px !important;
  background: transparent !important;
}

/* 侧边栏主面板：磨砂玻璃 */
.sidebar-content,
.sidebar-pane nav {
  background: linear-gradient(
    180deg,
    rgba(20, 21, 30, 0.85) 0%,
    rgba(15, 16, 24, 0.9) 100%
  ) !important;
  backdrop-filter: blur(20px) saturate(160%) !important;
  -webkit-backdrop-filter: blur(20px) saturate(160%) !important;
  margin: 0 12px !important;
  border-radius: var(--radius-lg) !important;
  border: 1px solid rgba(255, 255, 255, 0.07) !important;
  padding: 20px 8px !important;
  box-shadow: 0 15px 35px rgba(0, 0, 0, 0.5),
              inset 0 1px 0 rgba(255, 255, 255, 0.05) !important;
  height: fit-content !important;
  max-height: 78vh !important;
  overflow-y: auto !important;
  /* 自定义滚动条 */
  scrollbar-width: thin !important;
  scrollbar-color: rgba(167, 139, 250, 0.3) transparent !important;
}

/* Webkit 滚动条 */
.sidebar-content::-webkit-scrollbar,
.sidebar-pane nav::-webkit-scrollbar {
  width: 4px;
}

.sidebar-content::-webkit-scrollbar-track,
.sidebar-pane nav::-webkit-scrollbar-track {
  background: transparent;
}

.sidebar-content::-webkit-scrollbar-thumb,
.sidebar-pane nav::-webkit-scrollbar-thumb {
  background: rgba(167, 139, 250, 0.3);
  border-radius: 2px;
}

.sidebar-content::-webkit-scrollbar-thumb:hover,
.sidebar-pane nav::-webkit-scrollbar-thumb:hover {
  background: rgba(167, 139, 250, 0.5);
}

/* 侧边栏分组标题 */
.sidebar-pane summary,
.sidebar-pane .group-label,
details > summary {
  font-size: 0.7rem !important;
  font-weight: 700 !important;
  letter-spacing: 0.1em !important;
  text-transform: uppercase !important;
  color: rgba(167, 139, 250, 0.5) !important;
  padding: 12px 16px 6px !important;
  display: flex !important;
  align-items: center !important;
  gap: 8px !important;
}

/* 分组标题装饰线 */
.sidebar-pane summary::after,
.sidebar-pane .group-label::after {
  content: "";
  flex: 1;
  height: 1px;
  background: linear-gradient(90deg, rgba(167, 139, 250, 0.2), transparent);
}

/* 链接基础样式 */
.sidebar-pane a {
  display: flex !important;
  align-items: center !important;
  border-radius: var(--radius-md) !important;
  margin: 2px 8px !important;
  padding: 9px 16px !important;
  transition: var(--transition-fast) !important;
  color: rgba(148, 163, 184, 0.8) !important;
  font-size: 0.875rem !important;
  font-weight: 400 !important;
  position: relative !important;
  overflow: hidden !important;
  border: 1px solid transparent !important;
  text-decoration: none !important;
}

/* 链接 Hover：紫色渗透 */
.sidebar-pane a:hover:not([aria-current='page']) {
  background: rgba(167, 139, 250, 0.08) !important;
  color: rgba(221, 214, 254, 0.9) !important;
  transform: translateX(4px) !important;
  border-color: rgba(167, 139, 250, 0.12) !important;
}

/* 当前页：流光高亮 */
.sidebar-pane a[aria-current='page'] {
  background: linear-gradient(
    90deg,
    rgba(167, 139, 250, 0.18) 0%,
    rgba(167, 139, 250, 0.06) 100%
  ) !important;
  color: #c4b5fd !important;
  border-color: rgba(167, 139, 250, 0.25) !important;
  font-weight: 600 !important;
  box-shadow: inset 0 0 12px rgba(167, 139, 250, 0.08) !important;
}

/* 当前页左侧发光竖线 */
.sidebar-pane a[aria-current='page']::before {
  content: "";
  position: absolute;
  left: 0;
  top: 20%;
  height: 60%;
  width: 2px;
  background: linear-gradient(180deg, transparent, #a78bfa, transparent);
  border-radius: 2px;
}

/* 当前页扫光动效 */
.sidebar-pane a[aria-current='page']::after {
  content: "";
  position: absolute;
  top: 0;
  left: -100%;
  width: 50%;
  height: 100%;
  background: linear-gradient(
    90deg,
    transparent,
    rgba(167, 139, 250, 0.12),
    transparent
  );
  animation: sidebarGlow 3s ease-in-out infinite;
}

@keyframes sidebarGlow {
  0% { left: -100%; }
  100% { left: 200%; }
}

/* ======================================================
   6. 引用块升级（底部名言）
   ====================================================== */
blockquote {
  position: relative !important;
  background: rgba(167, 139, 250, 0.05) !important;
  border: none !important;
  border-radius: var(--radius-lg) !important;
  padding: 24px 32px 24px 48px !important;
  margin: 2rem 0 !important;
  backdrop-filter: blur(8px) !important;
  box-shadow: inset 0 0 30px rgba(167, 139, 250, 0.05) !important;
}

blockquote::before {
  content: '"';
  position: absolute;
  left: 16px;
  top: 8px;
  font-size: 3rem;
  line-height: 1;
  color: rgba(167, 139, 250, 0.4);
  font-family: Georgia, serif;
}

blockquote p {
  color: rgba(196, 181, 253, 0.8) !important;
  font-style: italic !important;
  font-size: 1rem !important;
  line-height: 1.7 !important;
}

/* ======================================================
   7. 电影颗粒底噪
   ====================================================== */
body::after {
  content: "";
  position: fixed;
  top: 0;
  left: 0;
  width: 100vw;
  height: 100vh;
  background-image: url("data:image/png;base64,");
  opacity: 0.02;
  pointer-events: none;
  z-index: 9999;
}

/* ======================================================
   8. 移动端适配
   ====================================================== */
@media (max-width: 50rem) {
  div.header {
    top: 0 !important;
    left: 0 !important;
    right: 0 !important;
    transform: none !important;
    width: 100% !important;
    border-radius: 0 !important;
    padding: 0 16px !important;
    border: none !important;
    border-bottom: 1px solid var(--glass-border) !important;
  }

  .sidebar-pane {
    padding-top: 0 !important;
  }

  .hero {
    padding: 4rem 1rem 3rem !important;
  }

  .hero h1 {
    font-size: clamp(2rem, 8vw, 3rem) !important;
  }
}
